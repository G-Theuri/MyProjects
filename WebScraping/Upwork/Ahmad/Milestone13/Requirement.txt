Step 1: Install Python
Step 2: Install Required Dependencies:
    (a) scrapy- i.e. <pip install scrapy>
    (b) Rich-  i.e. <pip install rich>
    (c) lxml-  i.e. <pip install lxml>
    (d) undetected_chromedriver-  i.e. <pip install undetected-chromedriver>
    (e) selenium -  i.e. <pip install selenium>

